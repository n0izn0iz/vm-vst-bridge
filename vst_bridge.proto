syntax = "proto3";

option go_package = ".;main";

package vstbridge;

// Interface exported by the server.
service VSTBridge {
  rpc Echo(Echo.Request) returns (Echo.Reply) {}
}

message Echo {
  message Request {
    string str = 1;
  }
  message Reply {
    string str = 1;
  }
}
